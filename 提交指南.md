# 扫地僧 · Edge Add-ons 上架提交指南

> 包与素材都准备好了。本文档按 Partner Center 的 8 步流程顺序排列，每一格都是要复制粘贴或上传的成品。
> 直接登录 https://partner.microsoft.com/en-us/dashboard/microsoftedge/overview 开干。

---

## 0. 待上传素材速查

| 用途 | 路径 | 规格 |
|:--|:--|:--|
| 扩展包 | `dist/scourgify-edge-v1.0.0.zip`（76 KB） | zip，根目录含 `manifest.json` |
| 商店 Logo | `assets/store/logo-300.png` | 300×300，PNG |
| Logo 备用 | `assets/store/logo-128.png` | 128×128，PNG |
| 小磁贴 | `assets/store/tile-440x280.png` | 440×280，PNG |
| 大磁贴 | `assets/store/tile-1400x560.png` | 1400×560，PNG |
| 截图 1（占位） | `assets/store/screenshot-1280x800.png` | 1280×800，PNG |

> 截图目前只有 1 张占位，品牌卡风格。过审后随时在 Partner Center 再加 3-5 张真实效果截图。

---

## 1. 准备要提交的扩展

- 已在 `https://partner.microsoft.com` 用你的 Microsoft 账号登录 ✅
- 扩展 zip 与上面 6 张素材在本地 ✅

---

## 2. 在 Partner Center 创建新扩展

> Workspaces → Edge 卡片 → Overview → **Create new extension**

---

## 3. 上传扩展包

> 上传 `dist/scourgify-edge-v1.0.0.zip`（项目根目录 `dist/` 内）
>
> 平台验证后会从 manifest 读取出 name / version / description。验证不通过时优先检查 `icons` 路径是否都在 zip 内、version 是否合法。

---

## 4. Availability

| 项 | 选 |
|:--|:--|
| Visibility | **Public**（默认） |
| Markets | 不动（默认全部） |

---

## 5. Properties

| 字段 | 值 |
|:--|:--|
| Category | **Productivity**（生产效率）|
| Website（可选） | `https://github.com/kakaoracle/scourgify` |
| Support contact | `https://github.com/kakaoracle/scourgify/issues` |
| Mature content | ☐ 不勾选 |

---

## 6. Privacy information

### Single purpose description（必填）

> 把网页里的杂音扫走，让正文回到主位。一把扫帚走江湖。

### Justify any permissions（必填）

> **`storage`**：跨设备同步用户在 popup 设置的功能开关、组折叠状态、CSDN 内容位置偏好；本地缓存广告过滤订阅源；为钥匙串（默认关闭）提供本地凭据存储区。
>
> **`alarms`**：以 12 小时为周期，在 Service Worker 内触发广告过滤订阅源的本地刷新（仅 `fetch` 拉取规则**文本**，不下载/执行任何远端代码）。
>
> **`host_permissions`（共 10 个域）**：
> - `spa.fenbi.com / www.fenbi.com / tiku.fenbi.com / ke.fenbi.com / keapi.fenbi.com`：粉笔页面增强与「AI 刷题班」跨域拉数（仅 DOM/CSS 修改 + 公开接口 GET）。
> - `blog.csdn.net / www.zhihu.com`：CSDN / 知乎页面增强（DOM 隐藏 + 样式调整）。
> - `easylist-downloads.adblockplus.org / adrules.top / raw.githubusercontent.com`：拉取 EasyList China 与 AdRules 公开订阅源（纯文本规则，已做字符白名单过滤，不 eval、不注入脚本）。
>
> **content_scripts 匹配 `http(s)://*/*`**：
> - 第一个脚本（`document_idle`）：在所有页面注入基础广告隐藏样式与订阅规则生成的 CSS 选择器；
> - 第二个脚本（`document_start` + `all_frames` + `match_about_blank`，独立 `autofill.js`）：仅当用户在 popup 启用「钥匙串」时，才会改写登录表单的 `autocomplete` 属性并兜底填入本地凭据；**默认关闭**，不会读取或回传任何凭据到远端。

### Declare any use of remote code（必填）

> **No, I am not using remote code.**
>
> 扩展只从公开订阅源以 HTTPS GET 拉取**纯文本格式**的广告过滤规则文件（`easylistchina.txt` / `adblock.txt`），由 background.js 在本地解析为 CSS 选择器集合。规则进入 storage 前已通过白名单校验（拒绝含 `{};@<>\` 与 uBO 过程性伪类的条目），**不进行任何 `eval`、不通过 `innerHTML` 注入、不使用 `Function` 构造器**。

### Certify your data usage practices（必填）

> **勾选**：*None of the above*（即：不收集、不传输、不与开发者共享任何用户数据）。
>
> 本扩展：
> - 不维护任何自有服务器，不向开发者端点发送数据。
> - 唯一主动发起的网络请求是 `storage` 段说明的订阅源与粉笔公开接口，第三方仅能观察到浏览器 IP 与 UA，与本扩展无任何身份关联。
> - 钥匙串保存的账号密码只写入 `chrome.storage.local`，**默认关闭**，关闭开关即停一切读写。

### Privacy policy URL（必填）

> `https://github.com/kakaoracle/scourgify/blob/master/PRIVACY.md`

---

## 7. Store listing details（语言：en-US + zh-CN）

> 当前 manifest 是中文优先；建议至少在 en-US 上把描述也填一份，商店自动 cross-list。

### Extension name（已从 manifest 带入，只读）

> `扫地僧`

### Short description（从 manifest 的 description 带入，可在 Partner Center 覆盖）

> 扫地僧：网页阅读净化工具，一把扫帚走江湖。

### Description（长描述，每种语言必填，建议 ≥ 250 字符）— **zh-CN**

```markdown
## 扫地僧 — 网页阅读净化工具，一把扫帚走江湖

一把看不见的小扫帚，把网页里藏起来的杂物请到门外，正文终于坐上主位。

### 🌸 粉笔小径
- 强开解析与背题小径里被折叠的评论
- 一键隐藏解析视频、切换页面黑白
- 「AI 刷题班优化」：隐藏数字人；学习规划提供周次切换、学习分区、任务时长分布、可交互能力雷达图与阶段路径；排行榜复用官方 H5

### 🔵 蔚蓝书房（CSDN）
- 只展示博文、隐藏侧栏与推荐位
- 页面黑白、内容位置（左/中/右）随你

### 💙 蓝湖秘境（知乎）
- 只展示评论区、隐藏答案列表冗余信息
- 页面黑白可选

### 🧹 扫尘走廊（通用广告过滤）
- 内置基础规则 + 公开订阅源（EasyList China + AdRules），12 小时自动更新
- 个性化路由表（ad-routes.js）按站点精修，单条规则可启用/禁用
- 关闭 popup「广告过滤」开关即停一切拉取

### 🔑 钥匙串（密码自动填充）
- 两层：先解开 `autocomplete` 符文（不保存任何东西），再在原生仍不填时本地兜底
- 凭据**只存浏览器本地**（`chrome.storage.local`，明文），**默认关闭**
- 关闭开关即停一切读写并清空页面残留 UI

### 🔮 杂项
- JSON 美化：一键新标签页打开 `json.html`
- 页面黑白、隐藏视频：所有开关在 popup
- 全部功能均可在 popup 内按组折叠

### 🔒 隐私
完整数据使用与远程请求说明见 PRIVACY.md。不维护任何自有服务器，钥匙串默认关闭，凭据不联网。
```

### Description — **en-US**（同步上架建议）

```markdown
## 扫地僧 (Saodi Seng) — Web Reading Cleaner

A quiet sweeping monk that clears the noise out of web pages so the article can sit in the main seat again.

### Features
- **Fenbi**: uncollapse hidden comments, hide solution videos, monochrome mode, and an "AI Lecture optimization" that overlays study-plan week switcher, task duration distribution, interactive radar chart and stage path on top of the official H5.
- **CSDN**: posts only, hide sidebars and recommendation cards, monochrome mode, content position picker.
- **Zhihu**: comments only, hide feed noise, monochrome mode.
- **Universal ad filter**: built-in baseline + EasyList China + AdRules subscriptions (auto-refreshed every 12 h, fully disableable in the popup), plus a per-site routing table in ad-routes.js.
- **Keychain (password autofill)**: two layers — first removes the `autocomplete` glyphs so Chrome/Edge can do its job, then a local-only credential fallback for stubborn sites. **Default OFF**. Credentials are stored plaintext in `chrome.storage.local` and never leave your device.
- **JSON beautifier**, monochrome, hide video — every switch lives in the popup.

### Privacy
No own server. No telemetry. See PRIVACY.md for the full data handling and outbound request list.
```

### 搜索词（最多 7 个，每个 ≤ 30 字符）

| # | term | chars |
|:-:|:--|:-:|
| 1 | `粉笔` | 2 |
| 2 | `CSDN` | 4 |
| 3 | `知乎` | 2 |
| 4 | `广告过滤` | 4 |
| 5 | `JSON 美化` | 7（含空格）|
| 6 | `密码自动填充` | 6 |
| 7 | `页面净化` | 4 |

### Logo / Tiles / Screenshot

> 在 Store listing → Details for `zh-CN` 与 Details for `en-US` 各上传一次（点 Duplicate 可一键复制到其他语言）：
>
> - **Extension logo**：`assets/store/logo-300.png`（1:1，300×300）
> - **Small promotional tile**：`assets/store/tile-440x280.png`（440×280，可选）
> - **Large promotional tile**：`assets/store/tile-1400x560.png`（1400×560，可选）
> - **Screenshots**：`assets/store/screenshot-1280x800.png`（1280×800，可加图说「品牌封面占位」）
>
> 截图建议随后追加 2-4 张真实运行截图（粉笔解析页净化效果、CSDN 净化效果、知乎评论区效果、popup 开关面板）。

### YouTube video URL（可选）

> 暂不填

---

## 8. 认证测试说明 / Submit

> Notes for certification 框里粘下面这段，审核员照着点能省掉很多往返：

```text
测试说明：

1. 安装后默认开启：粉笔 / CSDN / 知乎 / 通用广告过滤；默认关闭：钥匙串（密码自动填充）。
2. 访问 https://blog.csdn.net/ 任意博文，可看到侧栏与推荐位被隐藏，正文居中（可在 popup 选「内容位置 → 偏左/居中/偏右」）。
3. 访问 https://www.zhihu.com/ 任意问题页，可看到「只展示评论」开关启用时页面只剩评论区。
4. 访问 https://spa.fenbi.com/ti/xxx 解析页（无需登录也可看到基础净化），「强开评论」开启后底部评论区会被展开。
5. 通用广告过滤不需要登录，打开 https://www.baidu.com/ 可见顶部与右侧广告位被隐藏；关闭 popup「广告过滤」开关后，规则拉取停止，已加载的规则仍生效直到下次刷新。
6. 钥匙串测试（可选，需登录态）：
   - 在 popup 「通用 → 密码自动填充」打开钥匙串开关。
   - 在任意带登录表单的页面登录一次（建议用测试账号），下次聚焦密码框即自动填充；多个账号时密码框右侧会出现「填充」按钮。
   - 关闭钥匙串开关，刷新页面后所有钥匙串 UI 消失。
7. JSON 美化：popup → 前端页面工具 → JSON 美化开关开启后，自动新标签页打开 chrome-extension://<id>/json.html。
8. 订阅源拉取逻辑：安装后立即拉一次（后台 service worker），每 12 小时一次（chrome.alarms）；关闭 popup「广告过滤」开关后不会发起新的拉取请求。

权限与远程代码说明（备查）：
- 本扩展不向任何自有服务器发送数据。唯一主动发起的请求是对 EasyList China / AdRules 公开订阅源以及粉笔公开接口的 HTTPS GET。
- 这些请求只拉取纯文本规则/数据，全部在本地解析；不进行 eval、不通过 innerHTML 注入远端代码、不通过 Function() 构造器执行远端内容。
- 钥匙串凭据明文存在 chrome.storage.local，默认关闭，不联网。

如需进一步确认远程拉取的安全性，可查看仓库 background.js 中 AD_SELECTOR_BAD / AD_SELECTOR_PROCEDURAL 的白名单逻辑。
```

> 点 **Submit** 等待审核，平均 1.5 天、最长 7 个工作日。

---

## 9. 提交后的回执与常见驳回

驳回邮件如果落在以下原因，可以直接按表回信：

| 驳回理由 | 回复要点 |
|:--|:--|
| Name is misleading / not single purpose | 「网页阅读净化」是唯一目的，粉笔/CSDN/知乎/广告过滤/钥匙串/JSON 都是这条主线下子能力；展示名已尽量精简为「扫地僧」 |
| Permission not justified | 见第 6 步「Justify any permissions」原文，按权限一一对应 |
| Privacy policy required | 已在仓库根目录提供 PRIVACY.md，已填入第 6 步的 URL |
| Trademark concern (哈利波特) | 已主动改名「扫地僧」，移除全部 IP 引用，README 与 popup 已同步更新 |
| Remote code concern | 我们使用 `No, I am not using remote code`，附上 background.js 的白名单逻辑链接；如审核员坚持要重选 Yes，可在认证备注里补一句「远端为纯文本规则，无 eval/innerHTML/Function」 |

---

## 10. 已就位文件清单

```
dist/scourgify-edge-v1.0.0.zip      ← 上传到 Partner Center 的包
assets/store/logo-300.png            ← 商店 Logo
assets/store/logo-128.png            ← 备用 Logo
assets/store/tile-440x280.png        ← 小磁贴
assets/store/tile-1400x560.png       ← 大磁贴
assets/store/screenshot-1280x800.png ← 截图（占位）

PRIVACY.md                           ← 隐私政策（已推到 GitHub）
README.md                            ← 已更新（加 edge 召唤仪式 + PRIVACY 链接）
manifest.json                        ← 已更新（name/description/icons/version 1.0.0）
popup.html                           ← 已更新（title/h1 → 扫地僧）
```

需要我接着用浏览器自动化帮你填 Partner Center 表单的话，回我一声就开。
